# Docker Deployment & Lifecycle

| Command | Explanation |
|---|---|
| `docker ps` | Lists all currently running containers |
| `docker stop my-nginx` | Stops the running Nginx container gracefully |
| `docker ps -a` | Lists all containers, including stopped ones, to confirm it exited |
| `docker rm my-nginx` | Permanently removes the stopped container |
